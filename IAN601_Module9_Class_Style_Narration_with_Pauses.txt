Slide 1: NumPy

Welcome to Module 9. This module focuses on NumPy. We will connect the new library tools to the Python foundations from earlier modules.

[pause=0.7]

NumPy helps Python work efficiently with numeric arrays, vectorized calculations, and matrix-shaped data.

[pause=1.0]

Slide 2: How to use this page

Use this lesson interactively. Read the idea, predict the shape or result, run the example, and then change one value so you can compare the output.

[pause=0.7]

Start with the main idea on the slide. Execute the Python code in the browser. Connect the output to the concept.

[pause=0.7]

Change one value, variable, or condition. Apply the pattern to a new example.

[pause=1.0]

Slide 3: Learning path for Module 9

This slide is the roadmap for Module 9. Follow the progression from the basic structure to selection, transformation, and a small analytical workflow.

[pause=0.7]

Create numeric arrays. Inspect dimensions and types. Calculate without manual loops.

[pause=0.7]

Select rows, columns, and masks. Combine arrays and scalars. Build numeric features for analytics.

[pause=1.0]

Slide 4: Why NumPy matters

This slide focuses on Why NumPy matters. Start with the data structure and what the operation is supposed to accomplish before looking closely at the syntax.

[pause=0.7]

A Python list can store many values. A NumPy array is designed for fast numeric work.

[pause=1.0]

Slide 5: Create a NumPy array

This slide focuses on Create a NumPy array. Start with the data structure and what the operation is supposed to accomplish before looking closely at the syntax.

[pause=0.7]

The common alias for NumPy is np . Arrays can be created from Python lists. Create and inspect an array

[pause=0.8]

Before running the example, predict the structure and output. Run it in the browser, inspect the values, dimensions, labels, or summary, then change one input and run it again. Focus on how the operation changes the data rather than reading the code line by line.

[pause=1.0]

Slide 6: Vectorized arithmetic

This slide focuses on Vectorized arithmetic. Start with the data structure and what the operation is supposed to accomplish before looking closely at the syntax.

[pause=0.7]

Vectorized operations apply one calculation to every value in an array. Apply one formula to all values

[pause=0.8]

Before running the example, predict the structure and output. Run it in the browser, inspect the values, dimensions, labels, or summary, then change one input and run it again. Focus on how the operation changes the data rather than reading the code line by line.

[pause=1.0]

Slide 7: Indexing and slicing

This slide focuses on Indexing and slicing. Start with the data structure and what the operation is supposed to accomplish before looking closely at the syntax.

[pause=0.7]

NumPy uses zero-based indexing like Python lists. Slices select ranges. Select array values

[pause=0.8]

Before running the example, predict the structure and output. Run it in the browser, inspect the values, dimensions, labels, or summary, then change one input and run it again. Focus on how the operation changes the data rather than reading the code line by line.

[pause=1.0]

Slide 8: Boolean masks

This slide focuses on Boolean masks. Start with the data structure and what the operation is supposed to accomplish before looking closely at the syntax.

[pause=0.7]

A boolean mask keeps values that meet a condition. Filter with a condition

[pause=0.8]

Before running the example, predict the structure and output. Run it in the browser, inspect the values, dimensions, labels, or summary, then change one input and run it again. Focus on how the operation changes the data rather than reading the code line by line.

[pause=1.0]

Slide 9: Two-dimensional arrays

This slide focuses on Two-dimensional arrays. Start with the data structure and what the operation is supposed to accomplish before looking closely at the syntax.

[pause=0.7]

A 2-D array looks like a table of numbers. Axis 0 usually means down columns. Axis 1 usually means across rows. Rows, columns, and axes

[pause=0.8]

Before running the example, predict the structure and output. Run it in the browser, inspect the values, dimensions, labels, or summary, then change one input and run it again. Focus on how the operation changes the data rather than reading the code line by line.

[pause=1.0]

Slide 10: Broadcasting

This slide focuses on Broadcasting. Start with the data structure and what the operation is supposed to accomplish before looking closely at the syntax.

[pause=0.7]

Broadcasting lets NumPy combine arrays with scalars or compatible shapes. Subtract column averages

[pause=0.8]

Before running the example, predict the structure and output. Run it in the browser, inspect the values, dimensions, labels, or summary, then change one input and run it again. Focus on how the operation changes the data rather than reading the code line by line.

[pause=1.0]

Slide 11: Random arrays with a seed

This slide focuses on Random arrays with a seed. Start with the data structure and what the operation is supposed to accomplish before looking closely at the syntax.

[pause=0.7]

NumPy can generate random data for simulations and teaching examples. A seed makes results repeatable. Generate repeatable random data

[pause=0.8]

Before running the example, predict the structure and output. Run it in the browser, inspect the values, dimensions, labels, or summary, then change one input and run it again. Focus on how the operation changes the data rather than reading the code line by line.

[pause=1.0]

Slide 12: Mini NumPy workflow

This slide focuses on Mini NumPy workflow. Start with the data structure and what the operation is supposed to accomplish before looking closely at the syntax.

[pause=0.7]

This example calculates a simple standardized score using the mean and standard deviation. Standardize values

[pause=0.8]

Before running the example, predict the structure and output. Run it in the browser, inspect the values, dimensions, labels, or summary, then change one input and run it again. Focus on how the operation changes the data rather than reading the code line by line.

[pause=1.0]

Slide 13: Module 9 wrap-up

This slide summarizes Module 9. Focus on the operations you should recognize and the situations where they make analytical work easier.

[pause=0.7]

Create numeric arrays. Shape and dtype Inspect array structure.

[pause=0.7]

Apply formulas to all values. Filter values with conditions. Work with rows and columns.

[pause=1.0]